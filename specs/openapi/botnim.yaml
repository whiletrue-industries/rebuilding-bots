{
  "openapi": "3.1.0",
  "info": {
    "title": "Get botnim related information",
    "description": "Retrieves various botnim related information from the botnim api",
    "version": "v1.2.0"
  },
  "servers": [
    {
      "url": "https://botnim.staging.build-up.team"
    }
  ],
  "paths": {
    "/botnim/retrieve/budgetkey/common_knowledge__dev": {
      "get": {
        "description": "Full text search on the budgetkey common knowledge",
        "operationId": "search_budgetkey__common_knowledge__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/common_budget_knowledge__dev": {
      "get": {
        "description": "Search general budget knowledge in the unified knowledge base",
        "operationId": "search_unified__common_budget_knowledge__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "REGULAR",
                "METADATA_BROWSE"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/common_takanon_knowledge__dev": {
      "get": {
        "description": "Search general Knesset procedures and explanations in the unified knowledge base",
        "operationId": "search_unified__common_takanon_knowledge__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "REGULAR",
                "METADATA_BROWSE"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/legal_text__dev": {
      "get": {
        "description": "Search laws, bylaws and regulations in the unified legal knowledge base",
        "operationId": "search_unified__legal_text__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "SECTION_NUMBER",
                "REGULAR",
                "METADATA_BROWSE"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/legal_advisor_opinions__dev": {
      "get": {
        "description": "Search formal legal opinions by Knesset legal advisor. THREE modes — choose by intent: (a) 'מה ההנחיות/חוות הדעת האחרונות', 'הכי עדכניות', 'הכי חדשות', 'מה התפרסם לאחרונה' → use search_mode=RECENCY_BROWSE: returns the calendar-newest documents in pure date-desc order (no similarity ranking, immune to query-phrasing bias). (b) 'אילו חוות דעת יש על X', 'תן לי רשימה של…', browse/enumerate intent on a TOPIC → search_mode=METADATA_BROWSE: similarity-ranked summaries (25 docs). (c) substantive content question requiring full text → leave search_mode=REGULAR (default). For (a) and (b), present 3–5 items with title, date, one-line subject, and קישור_למקור; do not re-sort RECENCY_BROWSE results — they are already newest-first.",
        "operationId": "search_unified__legal_advisor_opinions__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query. RECENCY_BROWSE ignores this — pass any short string (e.g. 'הנחיות'). REGULAR and METADATA_BROWSE both use it for ranking.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "REGULAR (default, 7 full-text chunks) for substantive content questions. METADATA_BROWSE (25 similarity-ranked metadata summaries) for topical browse/enumerate. RECENCY_BROWSE (10 calendar-newest documents, no similarity) — REQUIRED when the user asks for 'latest/most-recent/newest' without a strong topical filter, because METADATA_BROWSE's similarity ranking can miss the calendar-newest docs.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "RECENCY_BROWSE",
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/legal_advisor_letters__dev": {
      "get": {
        "description": "Search correspondence with the Knesset legal advisor. THREE modes — choose by intent: (a) 'מה ההנחיות/המכתבים האחרונים', 'הכי עדכני', 'הכי חדשים', 'מה התפרסם לאחרונה' → use search_mode=RECENCY_BROWSE: returns the calendar-newest documents in pure date-desc order (no similarity ranking, immune to query-phrasing bias). (b) 'אילו מכתבים יש על X', 'תן לי רשימה של…', browse/enumerate intent on a TOPIC → search_mode=METADATA_BROWSE: similarity-ranked summaries (25 docs). (c) substantive content question requiring full text → leave search_mode=REGULAR (default). For (a) and (b), present 3–5 items with title, date, one-line subject, and קישור_למקור; do not re-sort RECENCY_BROWSE results — they are already newest-first.",
        "operationId": "search_unified__legal_advisor_letters__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query. RECENCY_BROWSE ignores this — pass any short string (e.g. 'מכתב'). REGULAR and METADATA_BROWSE both use it for ranking.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "REGULAR (default, 7 full-text chunks) for substantive content questions. METADATA_BROWSE (25 similarity-ranked metadata summaries) for topical browse/enumerate. RECENCY_BROWSE (10 calendar-newest documents, no similarity) — REQUIRED when the user asks for 'latest/most-recent/newest' without a strong topical filter, because METADATA_BROWSE's similarity ranking can miss the calendar-newest docs.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "RECENCY_BROWSE",
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/house_committee_decisions__dev": {
      "get": {
        "description": "Search Knesset committee decisions. THREE modes — choose by intent: (a) 'אילו החלטות התקבלו לאחרונה', 'מה ההחלטות האחרונות', 'הכי חדשות', 'מה ההחלטה האחרונה' → search_mode=RECENCY_BROWSE: results are returned strictly date-desc (SQL `ORDER BY doc_date DESC, id DESC`), so results[0] IS the newest decision. When the user asks for THE latest (singular), report results[0] verbatim — do NOT re-rank by inspecting dates yourself, do NOT pick a different item even if its date string looks 'rounder' or appears earlier in the list visually. When the user asks for the latest few, present them in the order received. (b) 'אילו החלטות יש על X', 'רשימת החלטות בנושא…', topical browse → search_mode=METADATA_BROWSE (25 similarity-ranked summaries). (c) substantive content question → leave REGULAR (default). Present 3–5 items with title, date, one-line subject, and קישור_למקור.",
        "operationId": "search_unified__house_committee_decisions__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query. RECENCY_BROWSE ignores this — pass any short string (e.g. 'החלטה').",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "REGULAR (default, 7 full-text chunks) for substantive content questions. METADATA_BROWSE (25 similarity-ranked metadata summaries) for topical browse/enumerate. RECENCY_BROWSE (10 calendar-newest documents, no similarity) — REQUIRED for 'latest/most-recent/newest' queries with no strong topical filter.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "RECENCY_BROWSE",
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/ethics_decisions__dev": {
      "get": {
        "description": "Search ethics committee decisions. THREE modes — choose by intent: (a) 'אילו החלטות אתיקה לאחרונה', 'הכי חדשות' → search_mode=RECENCY_BROWSE: calendar-newest rulings in pure date-desc order. (b) 'אילו החלטות אתיקה יש על X', topical browse → search_mode=METADATA_BROWSE. (c) substantive content question → leave REGULAR (default). Present 3–5 items with title, date, one-line subject, and קישור_למקור; do not re-sort RECENCY_BROWSE results.",
        "operationId": "search_unified__ethics_decisions__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query. RECENCY_BROWSE ignores this — pass any short string (e.g. 'אתיקה').",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "REGULAR (default, 7 full-text chunks) for substantive content questions. METADATA_BROWSE (25 similarity-ranked metadata summaries) for topical browse. RECENCY_BROWSE (10 calendar-newest documents, no similarity) — REQUIRED for 'latest/most-recent/newest' queries with no strong topical filter.",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "RECENCY_BROWSE",
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/knesset_protocols__dev": {
      "get": {
        "description": "Search transcripts of Knesset committee discussions and plenary proceedings (פרוטוקולי ועדה / דברי הכנסת). Each result is one speaker turn with metadata (committee, date, agenda item, speaker, party, role). Use for what-was-said queries, MK quotes, expert testimony, chair rulings.",
        "operationId": "search_unified__knesset_protocols__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/knesset/sessions": {
      "get": {
        "description": "Live Knesset plenary sessions in a date window — JIT call to knesset.gov.il OData (no caching). Use this for time-sensitive questions: 'מה הישיבה הבאה במליאה', 'מה היה בישיבה לפני', 'איזה ישיבות מתוכננות בשבועיים הקרובים'. For content-driven semantic queries ('which session covered חוק מימון מפלגות'), use search_unified__plenary_schedule__dev instead. Returns sessions ordered by StartDate ascending; when include_items=true (default) each session has its agenda items inline as `items: [...]`. Hebrew dates are added as StartDateHe/FinishDateHe. Every returned session carries a `source_url` field: the stenogram URL when a transcript has been published, otherwise the session-detail (agenda) URL at `https://www.knesset.gov.il/plenum/heb/sessionDet.aspx?SessionID=<X>`. Cite this URL verbatim when referring to a session in answers — never link to the bare Knesset homepage.",
        "operationId": "knesset_sessions_live",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "description": "Start of date window (inclusive), ISO 8601: YYYY-MM-DD or full ISO. Window must be ≤ 400 days.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "to",
            "in": "query",
            "description": "End of date window (exclusive), ISO 8601.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "include_items",
            "in": "query",
            "description": "Include each session's agenda items inline. Default true.",
            "required": false,
            "schema": {
              "type": "boolean",
              "default": true
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/plenary_schedule__dev": {
      "get": {
        "description": "Search the Knesset plenary sitting schedule (לוח מליאה / לו\"ז המליאה). Use for content-driven semantic queries on plenary sessions ('which session covered חוק מימון מפלגות'). For time-sensitive lookups ('next session', 'last session', date-window questions), prefer the live knesset_sessions_live operation. Each result is one (session, agenda-item) row sourced from the Knesset ParliamentInfo OData service via fap-time refresh.",
        "operationId": "search_unified__plenary_schedule__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          }
        ],
        "deprecated": false
      }
    },
    "/botnim/retrieve/unified/government_decisions__dev": {
      "get": {
        "description": "Search Israeli cabinet (government) decisions from gov.il / Ma'agar HaHachlatot. Use for cabinet resolutions, government decisions on legislation, ministerial appointments, decisions on Knesset bills (support/oppose at preliminary reading), and policy decisions. Each result includes a procedure_number_str (the official decision number) and source_url for citation — always use source_url verbatim from the result, never construct or guess a URL. NOTE: Decision numbers recycle across coalition terms; decision 550 exists in 7 different governments. When a user asks about a decision by number without specifying a government term, use metadata_filter with only decision_number to retrieve all matching decisions, then present each distinct one (grouped by government term, date, and title) and ask which term the user wants to explore. Only filter by government_number if the user explicitly specifies a term.",
        "operationId": "search_unified__government_decisions__dev",
        "parameters": [
          {
            "name": "query",
            "in": "query",
            "description": "Free text search query",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "search_mode",
            "in": "query",
            "description": "Search mode for different types of queries",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "METADATA_BROWSE",
                "REGULAR"
              ],
              "default": "REGULAR"
            }
          },
          {
            "name": "num_results",
            "in": "query",
            "description": "Number of results to return",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            }
          },
          {
            "name": "metadata_filter",
            "in": "query",
            "description": "JSON object for JSONB exact-match filter on document metadata. IMPORTANT: Government decision numbers recycle across coalition terms — decision 550 exists in governments 28, 29, 30, 32, 35, 36, 37. Supported filter fields: 'decision_number' (string, e.g. \"550\") and 'government_number' (string, e.g. \"37\"). To search across ALL government terms for a given number (when the user has not specified a term), pass only decision_number: {\"decision_number\": \"550\"}. To narrow to a specific government term, combine both fields: {\"decision_number\": \"550\", \"government_number\": \"37\"}. Government numbers in dataset: 28 (ברק, 1999), 29–30 (שרון, 2001–2003), 31 (שרון+אולמרט, 2006), 32 (נתניהו, 2009), 33 (נתניהו, 2013), 34 (נתניהו, 2015), 35 (נתניהו, 2020), 36 (בנט, 2021), 37 (נתניהו, 2022+).",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "deprecated": false
      }
    }
  },
  "components": {
    "schemas": {}
  }
}
